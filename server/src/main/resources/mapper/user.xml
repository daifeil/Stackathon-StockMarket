<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockmarket.server.dao.UserMapper">
    <select id="getUserByUsername" resultType="com.stockmarket.server.entity.SimpleUserDO">
        select username, password, user_type from user where username =#{username} and confirmed=1
    </select>
    <update id="setUserConfirmed" parameterType="String">
        update user set confirmed=1  where username=#{username} and confirmed=0
    </update>
</mapper>
